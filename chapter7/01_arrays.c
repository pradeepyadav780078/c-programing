#include <stdio.h>

int main(){
   int marks[90];

   marks[0] = 45;
   marks[1] = 95;
   //we can go all the way till marks[89] 
   printf("marks of student %d is %d\n",marks[0], marks[1]);
    return 0;
}